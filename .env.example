# ============================================
# HumanTouch 环境变量配置
# ============================================

# --------------------------------------------
# LLM 提供商配置（二选一，优先级：OpenRouter > Custom）
# --------------------------------------------

# 方式一：OpenRouter (默认，支持 200+ 模型)
OPENROUTER_API_KEY=sk-or-your-openrouter-key
OPENROUTER_MODEL=google/gemini-2.5-flash-preview

# 方式二：自定义 OpenAI 兼容 API
# CUSTOM_LLM_API_KEY=your-api-key
# CUSTOM_LLM_BASE_URL=https://your-api.com/v1
# CUSTOM_LLM_MODEL=your-model-name

# --------------------------------------------
# AI 检测器 API（可选，不配置则使用模拟分数）
# --------------------------------------------
# ZEROGPT_API_KEY=your-zerogpt-key
# GPTZERO_API_KEY=your-gptzero-key
# COPYLEAKS_API_KEY=your-copyleaks-key

# 检测模式：mock（模拟）或 strict（严格，必须配置检测器 API）
DETECTOR_MODE=mock

# --------------------------------------------
# 认证配置（生产环境必需）
# --------------------------------------------
JWT_SECRET=your-jwt-secret-key-min-32-chars
ALLOWED_API_KEYS=hk_your_api_key_1,hk_your_api_key_2
API_KEY_PREFIX=hk_

# --------------------------------------------
# 应用配置
# --------------------------------------------
RATE_LIMIT_REQUESTS_PER_MINUTE=100
MAX_TEXT_LENGTH=10000

# 站点 URL（用于 OpenRouter 请求头）
SITE_URL=https://your-domain.com

# --------------------------------------------
# 部署配置
# --------------------------------------------
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://your-app-domain.vercel.app
